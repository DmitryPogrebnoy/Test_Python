dist: xenial
language: c++
os: linux
compiler: 
 - g++

python:
 - "3.7.1"

#virtualenv : 
  #system_site_packages :  true

#env:
  #global:
   # - PYTHONPATH="/home/travis/build/DmitryPogrebnoy/Test_Python"

addons:
  apt:
    packages:
     - python3-dev

before_install:
 #- sudo apt-get -y install python3-pip python3-dev
 - dir $HOME/virtualenv/python3.7.1
 - dir $HOME/virtualenv/python3.7.1/include/python3.7m
 - dir $HOME/virtualenv/python3.7.1/lib/python3.7
 #- dir $(pyenv root)/
 #- python3 -V
 #- pip3 -V
 - dir /home
 - dir /
 - dir /usr
 - dir /usr/include
 - dir /usr/lib
 - dir $HOME
 #- which python
 - echo $PYTHONPATH:$(pwd)
 - echo $PYTHONHOME
 - echo $PYTHONPATH
 #- export PYTHON=$PYTHONPATH
 #- echo $PYTHON
 - sudo apt-get install g++-mingw-w64-i686
 - sudo apt install qt5-default
#

script:
 - qmake Test_Python.pro
 - make
